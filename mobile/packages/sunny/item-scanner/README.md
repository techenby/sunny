# sunny/item-scanner

A NativePHP Mobile plugin that identifies the items in a photo with Apple's on-device Foundation Models. Nothing leaves the device.

Requires iOS 27 on an Apple Intelligence device. On anything else, `ItemScanner::availability()` reports why it can't run.

See `resources/boost/guidelines/core.blade.php` for usage.
